export type Status = 'online' | 'offline' | 'warning'
export type Metric = { timestamp: string; latencyMs: number; packetLossPercent: number; downloadMbps: number; uploadMbps: number; availabilityPercent: number; errorCount: number }
export type Device = { id: number; name: string; address: string; type: string; location: string; status: Status; monitoringInterval: number; lastChecked: string; latest: Metric }
export type Alert = { id: number; deviceId: number; deviceName: string; metric: string; value: number; threshold: number; severity: 'info' | 'warning' | 'critical'; message: string; status: 'active' | 'acknowledged' | 'resolved'; createdAt: string }
export type Dashboard = { mode: 'simulation' | 'live'; generatedAt: string; kpis: Record<string, { value: number; previous: number; unit: string; trend: number[] }>; health: { score: number; status: string; explanation: string; factors: Record<string, number> }; traffic: Metric[]; devices: Device[]; alerts: Alert[]; capacity?: { download: number; upload: number } | null; problematic: { device: Device; score: number; level: string }[] }
export type Statistics = { count: number; mean: number; median: number; mode: number | null; min: number; max: number; range: number; variance: number; standardDeviation: number; coefficientOfVariation: number; percentiles: Record<string, number>; trend: { direction: string; percentChange: number; movingAverage: number[] }; correlation: number; anomalies: { timestamp: string; value: number; expected: string; zScore: number; severity: string }[] }
